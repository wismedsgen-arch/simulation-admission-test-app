node_modules/
package-lock.json
.DS_Store
.agents/
skills-lock.json
testing
.next/
.cache/
.uploads/
.env
.env.*
!.env.example
.claude/

# TypeScript incremental build cache — regenerates on every tsc run
tsconfig.tsbuildinfo

# Local-only helper scripts (one-offs, not required to run the app)
scripts/
